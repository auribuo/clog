# clog

A simple yet powerful c logging libary. (Should(TM) also support c++)

The library is a single header and is capable of colored output and structured logging.

I am thinking of adding file logs in the future.

See `clog.h` or `main.c` for an example.

If you like it, leave a star and enjoy!
